name: "costwright check"
description: "Static budget certificates for LLM-agent workflows (LangGraph/CrewAI/Agents SDK)"
inputs:
  path:
    description: "Path to scan"
    default: "."
  fail-on:
    description: "Policy: reject | default-dependent | non-certifiable | none"
    default: "reject"
runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        pip install costwright
        if [ "${{ inputs.fail-on }}" = "none" ]; then
          costwright check "${{ inputs.path }}"
        else
          costwright check "${{ inputs.path }}" --fail-on "${{ inputs.fail-on }}"
        fi
