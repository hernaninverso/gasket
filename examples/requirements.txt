# Pinned BLACK-BOX dependency for the example demos ONLY (NOT a runtime dep of gasket-check / fusion).
# Window A owns the eleata-verify contract and guarantees it is additive-only, so pinning to the
# current origin/main is safe (see eleata-verify/docs/API-CONTRACT.md).
#
# PIN: eleata-verify origin/main @ b7a2c71 — the additive contract (verify().to_dict() gained only the
# optional relevance/gate fields; verdict/sla_mode enums unchanged) PLUS the ε estimator
# (eleata_verify.epsilon.interference_risk_bound) that examples/certified_run_demo.py consumes.
#
# eleata-verify is a private repo. Use the local editable install:
#     pip install -e ~/eleata-verify
# or, when published / for a pinned git install:
#     eleata-verify @ git+https://github.com/hernaninverso/eleata-verify.git@b7a2c71
numpy>=1.24
